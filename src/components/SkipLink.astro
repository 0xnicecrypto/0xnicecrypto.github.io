---
interface Props {
	label?: string;
}

const { label = "skip to content" } = Astro.props;
---

<a class="sr-only focus:not-sr-only focus:fixed focus:start-1 focus:top-1.5" href="#main">{label}</a>
